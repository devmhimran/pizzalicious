import PreLoader from "@/components/PreLoader/PreLoader";
import React from "react";

const Loading = () => {
  return (
    <div>
      <PreLoader />{" "}
    </div>
  );
};

export default Loading;
